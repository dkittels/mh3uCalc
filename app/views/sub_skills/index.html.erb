<h1>Sub Skills</h1>
<p>Sub Skills are the actual skills as they apply to the character once you've accrued enough
points in a skill.  Each skill can have one or more sub skills: for instance, Attack Up is a skill,
while Attack Up Small, Attack Up Medium, and Attack Up Large as sub-skills.
</p>
<table class="table">
	<thead>
	  <tr>
		<th>Name</th>
		<th>Skill</th>
		<th>Parent sub skill</th>
		<th>Skill value</th>
		<th></th>
		<th></th>
		<th></th>
	  </tr>
	</thead>
	<tbody>

<% @sub_skills.each do |sub_skill| %>
  <tr>
    <td><%= sub_skill.name %></td>
    <td><%= sub_skill.skill.name %></td>
    <td><%= sub_skill.parent_sub_skill_id %></td>
    <td><%= sub_skill.skill_value %></td>
    <td><%= link_to 'Show', sub_skill, {:class => 'btn btn-mini'} %></td>
    <td><%= link_to 'Edit', edit_sub_skill_path(sub_skill), {:class => 'btn btn-mini'} %></td>
    <td><%= link_to 'Destroy', sub_skill, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-mini btn-danger' %></td>
  </tr>
<% end %>
	
</table>

<br />

<%= link_to 'New Sub skill', new_sub_skill_path, {:class => 'btn btn-success'}  %>
